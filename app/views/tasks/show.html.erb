<h1>Task</h1>
<ul>
  <li><%= @task.title %></li>
    <p> <%= @task.details %></p>
    <p><%= @task.completed %></p>
</ul>


    <%= link_to 'Edit a task', edit_task_path %>

    <%= link_to 'Delete',
        task_path(@task),
        method: :delete,
        data: {confirm: "are you sure?"} %>

    <%= link_to 'Back', tasks_path %>
